# 입력 전용 페이지 생성 완료 보고서

**작업 일자**: 2026-01-24  
**목적**: 메뉴/재료/레시피를 입력 전용 페이지로 분리 (설계 기능 제거)

---

## 1️⃣ 변경 요약

### 변경 원칙
- **입력 전용 페이지 생성**: 설계 기능 없이 입력만 수행하는 페이지 생성
- **사이드바 라벨 변경**: "메뉴 관리" → "메뉴 입력", "재료 관리" → "재료 입력", "레시피 관리" → "레시피 입력"
- **Page key 변경**: "메뉴 등록" → "메뉴 입력", "재료 등록" → "재료 입력", "레시피 등록" → "레시피 입력"
- **기존 설계 페이지 유지**: "메뉴 등록", "재료 등록" 라우팅은 설계 페이지로 유지 (설계 그룹에서 접근)

---

## 2️⃣ 변경된 라벨 및 Page Key

### 사이드바 입력 그룹 (빠른 입력 expander)

| 변경 전 | 변경 후 | Page Key (변경) |
|---------|---------|----------------|
| 메뉴 관리 | 메뉴 입력 | "메뉴 입력" (신규) |
| 재료 관리 | 재료 입력 | "재료 입력" (신규) |
| 레시피 관리 | 레시피 입력 | "레시피 입력" (신규) |

### 입력 허브 버튼

| 변경 전 | 변경 후 | Page Key (변경) |
|---------|---------|----------------|
| 📘 메뉴 관리 | 📘 메뉴 입력 | "메뉴 입력" |
| 🧺 재료 관리 | 🧺 재료 입력 | "재료 입력" |
| 🧑‍🍳 레시피 관리 | 🧑‍🍳 레시피 입력 | "레시피 입력" |

---

## 3️⃣ 신규 생성된 파일

### 3.1 ui_pages/input/menu_input.py (신규)

**목적**: 메뉴 입력 전용 페이지 (설계 기능 없음)

**기능**:
- 단일 메뉴 입력 폼
- 일괄 메뉴 입력 폼
- 등록된 메뉴 목록 표시

**사용 함수**:
- `render_menu_input()`: 단일 입력 폼
- `render_menu_batch_input()`: 일괄 입력 폼
- `save_menu()`: 메뉴 저장
- `load_csv('menu_master.csv')`: 메뉴 목록 조회

### 3.2 ui_pages/input/ingredient_input.py (신규)

**목적**: 재료 입력 전용 페이지 (설계 기능 없음)

**기능**:
- 재료 입력 폼 (재료명, 단위, 단가, 발주 단위, 변환 비율)
- 등록된 재료 목록 표시

**사용 함수**:
- `render_ingredient_input()`: 재료 입력 폼
- `save_ingredient()`: 재료 저장
- `load_csv('ingredient_master.csv')`: 재료 목록 조회

### 3.3 ui_pages/recipe_management.py (수정)

**변경 내용**:
- 헤더 변경: "레시피 등록" → "🧑‍🍳 레시피 입력"
- 함수 docstring 변경: "레시피 등록 페이지" → "레시피 입력 페이지 렌더링 (입력 전용)"

**기능**: 이미 입력 전용으로 구현되어 있음 (일괄 레시피 등록)

---

## 4️⃣ 수정된 파일 목록

### 4.1 app.py

**위치**: 라인 1527-1529, 1800-1810, 330-351

**변경 내용**:
1. **menu_categories 라벨 변경**:
   - "메뉴 관리" → "메뉴 입력"
   - "재료 관리" → "재료 입력"
   - "레시피 관리" → "레시피 입력"

2. **라우팅 추가**:
   - `elif page == "메뉴 입력":` → `render_menu_input_page()`
   - `elif page == "재료 입력":` → `render_ingredient_input_page()`
   - `elif page == "레시피 입력":` → `render_recipe_management()`

3. **기존 라우팅 유지**:
   - `elif page == "메뉴 등록":` → 설계 페이지 (설계 그룹에서 접근)
   - `elif page == "재료 등록":` → 설계 페이지 (설계 그룹에서 접근)
   - `elif page == "레시피 등록":` → 기존 유지 (하위 호환)

### 4.2 ui_pages/input/input_hub.py

**위치**: 라인 330-351

**변경 내용**:
- 버튼 라벨 변경: "메뉴 관리" → "메뉴 입력", "재료 관리" → "재료 입력", "레시피 관리" → "레시피 입력"
- page key 변경: "메뉴 등록" → "메뉴 입력", "재료 등록" → "재료 입력", "레시피 등록" → "레시피 입력"
- 설명 문구 변경: "등록하고 관리합니다" → "등록합니다"

### 4.3 ui_pages/recipe_management.py

**위치**: 라인 15-17

**변경 내용**:
- 헤더 변경: "레시피 등록" → "🧑‍🍳 레시피 입력"
- 함수 docstring 변경: "레시피 등록 페이지" → "레시피 입력 페이지 렌더링 (입력 전용)"

---

## 5️⃣ 기능 영향 없음 체크리스트

### ✅ 기존 설계 페이지 유지
- [x] "메뉴 등록" 라우팅 유지 (설계 그룹에서 접근)
- [x] "재료 등록" 라우팅 유지 (설계 그룹에서 접근)
- [x] 설계 기능 정상 작동

### ✅ DB/저장 로직 유지
- [x] `save_menu()` 함수 변경 없음
- [x] `save_ingredient()` 함수 변경 없음
- [x] `save_recipe()` 함수 변경 없음
- [x] 테이블 구조 변경 없음

### ✅ 입력 함수 재사용
- [x] `render_menu_input()` 재사용
- [x] `render_menu_batch_input()` 재사용
- [x] `render_ingredient_input()` 재사용
- [x] 기존 입력 함수 변경 없음

---

## 6️⃣ 검증 체크리스트

### 사이드바 메뉴
- [ ] 입력 그룹에서 "메뉴 입력", "재료 입력", "레시피 입력" 표시됨
- [ ] 각 메뉴 클릭 시 입력 전용 페이지로 이동

### 입력 허브 버튼
- [ ] "📘 메뉴 입력", "🧺 재료 입력", "🧑‍🍳 레시피 입력" 버튼 표시됨
- [ ] 각 버튼 클릭 시 입력 전용 페이지로 이동

### 입력 전용 페이지
- [ ] 메뉴 입력 페이지: 단일 입력 / 일괄 입력 탭 표시
- [ ] 메뉴 입력 페이지: 저장 후 목록 갱신
- [ ] 재료 입력 페이지: 재료 입력 폼 표시
- [ ] 재료 입력 페이지: 저장 후 목록 갱신
- [ ] 레시피 입력 페이지: 일괄 레시피 등록 폼 표시
- [ ] 모든 페이지에서 설계 기능 없음 확인

---

## 7️⃣ 변경 상세 내역

### 7.1 app.py 변경

**menu_categories 라벨 변경**:
```python
# 변경 전
("메뉴 관리", "메뉴 등록"),
("재료 관리", "재료 등록"),
("레시피 관리", "레시피 등록"),

# 변경 후
("메뉴 입력", "메뉴 입력"),  # 라벨 변경, page key 변경
("재료 입력", "재료 입력"),  # 라벨 변경, page key 변경
("레시피 입력", "레시피 입력"),  # 라벨 변경, page key 변경
```

**라우팅 추가**:
```python
# 메뉴 입력 페이지 (입력 전용)
elif page == "메뉴 입력":
    from ui_pages.input.menu_input import render_menu_input_page
    render_menu_input_page()

# 재료 입력 페이지 (입력 전용)
elif page == "재료 입력":
    from ui_pages.input.ingredient_input import render_ingredient_input_page
    render_ingredient_input_page()

# 레시피 입력 페이지 (입력 전용)
elif page == "레시피 입력":
    from ui_pages.recipe_management import render_recipe_management
    render_recipe_management()
```

### 7.2 input_hub.py 변경

**버튼 라벨 및 page key 변경**:
```python
# 변경 전
st.markdown("**📘 메뉴 관리**")
st.caption("가게의 모든 메뉴를 등록하고 관리합니다")
if st.button("📘 메뉴 관리", ...):
    st.session_state["current_page"] = "메뉴 등록"

# 변경 후
st.markdown("**📘 메뉴 입력**")
st.caption("가게의 모든 메뉴를 등록합니다")
if st.button("📘 메뉴 입력", ...):
    st.session_state["current_page"] = "메뉴 입력"
```

### 7.3 신규 파일 생성

**menu_input.py**:
- 단일 입력 / 일괄 입력 탭 구조
- 저장 후 목록 갱신
- 입력 전용 (설계 기능 없음)

**ingredient_input.py**:
- 재료 입력 폼
- 저장 후 목록 갱신
- 입력 전용 (설계 기능 없음)

---

## 8️⃣ 설계 페이지와의 관계

**중요**: 기존 설계 페이지는 그대로 유지됩니다.

- **"메뉴 등록"** (page key): 설계 그룹에서 접근 → 메뉴 포트폴리오 설계실
- **"재료 등록"** (page key): 설계 그룹에서 접근 → 재료 구조 설계실
- **"레시피 등록"** (page key): 기존 유지 (하위 호환)

**입력 그룹**:
- **"메뉴 입력"** (page key): 입력 전용 페이지
- **"재료 입력"** (page key): 입력 전용 페이지
- **"레시피 입력"** (page key): 입력 전용 페이지

---

## 9️⃣ 테스트 시나리오

### 시나리오 1: 메뉴 입력 페이지
1. 입력 허브에서 "📘 메뉴 입력" 버튼 클릭
2. **예상 결과**: 메뉴 입력 페이지로 이동
3. 단일 입력 탭에서 메뉴명, 판매가 입력 후 저장
4. **예상 결과**: 저장 성공 메시지 + 목록 갱신
5. 일괄 입력 탭에서 여러 메뉴 입력 후 저장
6. **예상 결과**: 저장 성공 메시지 + 목록 갱신

### 시나리오 2: 재료 입력 페이지
1. 입력 허브에서 "🧺 재료 입력" 버튼 클릭
2. **예상 결과**: 재료 입력 페이지로 이동
3. 재료명, 단위, 단가 입력 후 저장
4. **예상 결과**: 저장 성공 메시지 + 목록 갱신

### 시나리오 3: 레시피 입력 페이지
1. 입력 허브에서 "🧑‍🍳 레시피 입력" 버튼 클릭
2. **예상 결과**: 레시피 입력 페이지로 이동
3. 메뉴 선택, 재료 개수 선택, 재료별 사용량 입력 후 저장
4. **예상 결과**: 저장 성공 메시지

### 시나리오 4: 설계 페이지 유지 확인
1. 설계 그룹에서 "메뉴 포트폴리오 설계" 클릭
2. **예상 결과**: 메뉴 포트폴리오 설계실 페이지 (설계 기능 포함)
3. 설계 그룹에서 "재료 구조 설계" 클릭
4. **예상 결과**: 재료 구조 설계실 페이지 (설계 기능 포함)

---

## 🔟 주의 사항

### 10.1 Page Key 변경

**중요**: 입력 그룹의 page key가 변경되었습니다.

- 기존: "메뉴 등록", "재료 등록", "레시피 등록"
- 신규: "메뉴 입력", "재료 입력", "레시피 입력"

**이유**: 입력 전용 페이지와 설계 페이지를 구분하기 위해

### 10.2 기존 설계 페이지 유지

**중요**: 기존 설계 페이지는 그대로 유지됩니다.

- "메뉴 등록" (page key): 설계 그룹에서 접근 → 메뉴 포트폴리오 설계실
- "재료 등록" (page key): 설계 그룹에서 접근 → 재료 구조 설계실

**이유**: 설계 기능은 설계 그룹에서 접근하도록 유지

### 10.3 입력 전용 페이지 특징

**입력 전용 페이지**:
- 설계 기능 없음 (분류, 분석, 전략 도구 등 제거)
- 입력 폼만 제공
- 저장 후 목록 갱신
- 간단하고 직관적인 UI

---

**작성일**: 2026-01-24  
**담당**: 입력 전용 페이지 생성 작업
