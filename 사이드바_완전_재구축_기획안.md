# 사이드바 완전 재구축 기획안

## 1. 현재 사이드바 구조 분석

### 1.1 현재 메뉴 구조

```python
menu = {
    "🏠 홈": [("홈", "홈")],
    "🧠 설계": {
        "main": [("가게 전략 센터", "가게 전략 센터")],
        "sub": [
            ("메뉴 포트폴리오 설계", "메뉴 등록"),
            ("메뉴 수익 설계", "메뉴 수익 구조 설계실"),
            ("재료 구조 설계", "재료 등록"),
            ("수익 구조 설계", "수익 구조 설계실"),
            ("레시피 설계", "레시피 등록")
        ]
    },
    "📊 분석": {
        "main": [("분석 허브", "분석 허브")],
        "sub": [
            ("매출 분석", "매출 관리"),
            ("판매·메뉴 분석", "판매 관리"),
            ("원가 분석", "비용 분석"),
            ("체크 결과 요약", "검진 결과 요약"),
            ("체크 히스토리", "검진 히스토리"),
            ("매출 하락 원인 찾기", "매출 하락 원인 찾기")
        ]
    },
    "✍ 입력": {
        "main": [("데이터 입력 센터", "입력 허브")],
        "sub": [
            ("오늘 마감 입력", "일일 입력(통합)"),
            ("매출/방문자 입력", "매출 등록"),
            ("판매량 입력", "판매량 등록"),
            ("월간 정산 입력", "실제정산"),
            ("비용 목표 입력", "목표 비용구조"),
            ("매출 목표 입력", "목표 매출구조"),
            ("QSC 입력", "건강검진 실시")
        ]
    },
    "🛠 운영": [
        ("직원 연락망", "직원 연락망"),
        ("협력사 연락망", "협력사 연락망"),
        ("게시판", "게시판")
    ]
}
```

### 1.2 현재 사이드바 기능

1. **토글 버튼**: "◀ 접기" / "▶ 펼치기"
2. **매장 선택**: 여러 매장이 있을 경우 selectbox
3. **카테고리별 메뉴**: 5개 카테고리 (홈, 설계, 분석, 입력, 운영)
4. **접힌 상태**: 아이콘만 표시
5. **로그아웃/캐시 클리어**: 하단 버튼

### 1.3 현재 문제점

1. **복잡한 JavaScript 충돌**: 여러 스크립트가 서로 간섭
2. **CSS 우선순위 문제**: Streamlit 기본 스타일과 충돌
3. **접기/펼치기 기능 불안정**: 상태 관리가 복잡함
4. **유지보수 어려움**: 코드가 분산되어 있음

## 2. 재구축 전략

### 2.1 핵심 원칙

1. **단순성**: 복잡한 JavaScript 최소화
2. **명확성**: 코드 구조 명확하게 분리
3. **안정성**: Streamlit 기본 기능 최대한 활용
4. **유지보수성**: 코드 중복 제거, 모듈화

### 2.2 접근 방법

1. **기존 사이드바 코드 완전 삭제**
2. **깨끗한 상태에서 재구축**
3. **최소한의 JavaScript 사용**
4. **CSS는 필수적인 것만 유지**

## 3. 새로운 사이드바 구조

### 3.1 기본 구조

```python
# 사이드바 상태 관리 (최소화)
if "sidebar_collapsed" not in st.session_state:
    st.session_state.sidebar_collapsed = False

# 메뉴 구조 정의 (기존과 동일)
menu = {
    "🏠 홈": [("홈", "홈")],
    "🧠 설계": {
        "main": [("가게 전략 센터", "가게 전략 센터")],
        "sub": [
            ("메뉴 포트폴리오 설계", "메뉴 등록"),
            ("메뉴 수익 설계", "메뉴 수익 구조 설계실"),
            ("재료 구조 설계", "재료 등록"),
            ("수익 구조 설계", "수익 구조 설계실"),
            ("레시피 설계", "레시피 등록")
        ]
    },
    "📊 분석": {
        "main": [("분석 허브", "분석 허브")],
        "sub": [
            ("매출 분석", "매출 관리"),
            ("판매·메뉴 분석", "판매 관리"),
            ("원가 분석", "비용 분석"),
            ("체크 결과 요약", "검진 결과 요약"),
            ("체크 히스토리", "검진 히스토리"),
            ("매출 하락 원인 찾기", "매출 하락 원인 찾기")
        ]
    },
    "✍ 입력": {
        "main": [("데이터 입력 센터", "입력 허브")],
        "sub": [
            ("오늘 마감 입력", "일일 입력(통합)"),
            ("매출/방문자 입력", "매출 등록"),
            ("판매량 입력", "판매량 등록"),
            ("월간 정산 입력", "실제정산"),
            ("비용 목표 입력", "목표 비용구조"),
            ("매출 목표 입력", "목표 매출구조"),
            ("QSC 입력", "건강검진 실시")
        ]
    },
    "🛠 운영": [
        ("직원 연락망", "직원 연락망"),
        ("협력사 연락망", "협력사 연락망"),
        ("게시판", "게시판")
    ]
}

# 사이드바 렌더링 함수
def render_sidebar():
    with st.sidebar:
        # 토글 버튼
        toggle_icon = "◀ 접기" if not st.session_state.sidebar_collapsed else "▶ 펼치기"
        if st.button(toggle_icon, key="sidebar_toggle", use_container_width=True):
            st.session_state.sidebar_collapsed = not st.session_state.sidebar_collapsed
            st.rerun()
        
        # 접힌 상태 처리
        if st.session_state.sidebar_collapsed:
            render_collapsed_sidebar(menu)
        else:
            render_expanded_sidebar(menu)

# 펼친 상태 렌더링
def render_expanded_sidebar(menu):
    # 매장 선택
    user_stores = get_user_stores()
    curr_name = get_current_store_name()
    if len(user_stores) > 1:
        store_options = {f"{s['name']} ({s['role']})": s['id'] for s in user_stores}
        selected = st.selectbox("🏪 매장 선택", options=list(store_options.keys()))
        if store_options[selected] != get_current_store_id():
            if switch_store(store_options[selected]): st.rerun()
    else:
        st.markdown(f"🏪 **{curr_name}**")
    
    # 메뉴 렌더링
    if "current_page" not in st.session_state:
        st.session_state.current_page = "홈"
    
    for cat, data in menu.items():
        st.markdown(f"**{cat}**")
        if isinstance(data, list):
            for label, key in data:
                if st.button(label, key=f"btn_{key}", use_container_width=True, 
                           type="primary" if st.session_state.current_page == key else "secondary"):
                    st.session_state.current_page = key
                    st.rerun()
        else:
            # Main items
            for label, key in data["main"]:
                if st.button(label, key=f"btn_{key}", use_container_width=True,
                           type="primary" if st.session_state.current_page == key else "secondary"):
                    st.session_state.current_page = key
                    st.rerun()
            # Sub items in expander
            with st.expander("상세 선택", expanded=False):
                for label, key in data["sub"]:
                    if st.button(label, key=f"btn_sub_{key}", use_container_width=True,
                               type="primary" if st.session_state.current_page == key else "secondary"):
                        st.session_state.current_page = key
                        st.rerun()
    
    # 로그아웃, 캐시 클리어
    st.markdown("<br>", unsafe_allow_html=True)
    if st.button("🚪 로그아웃"): 
        logout()
        st.rerun()
    if st.button("🔄 캐시 클리어"): 
        load_csv.clear()
        st.rerun()

# 접힌 상태 렌더링
def render_collapsed_sidebar(menu):
    category_icons = {
        "🏠 홈": "🏠",
        "🧠 설계": "🧠",
        "📊 분석": "📊",
        "✍ 입력": "✍",
        "🛠 운영": "🛠"
    }
    
    for cat, data in menu.items():
        icon = category_icons.get(cat, "📋")
        if st.button(icon, key=f"collapsed_{cat}", use_container_width=True, help=cat):
            st.session_state.sidebar_collapsed = False
            if isinstance(data, list):
                st.session_state.current_page = data[0][1]
            else:
                st.session_state.current_page = data["main"][0][1]
            st.rerun()
    
    st.markdown("<br>", unsafe_allow_html=True)
    if st.button("🚪", key="collapsed_logout", use_container_width=True, help="로그아웃"):
        logout()
        st.rerun()
    if st.button("🔄", key="collapsed_clear", use_container_width=True, help="캐시 클리어"):
        load_csv.clear()
        st.rerun()
```

### 3.2 CSS 구조 (최소화)

```css
/* 사이드바 기본 폭 */
[data-testid="stSidebar"] {
    width: 15rem !important;
    min-width: 15rem !important;
    max-width: 15rem !important;
    transition: width 0.3s ease !important;
}

/* 접힌 상태 */
[data-testid="stSidebar"].sidebar-collapsed {
    width: 4rem !important;
    min-width: 4rem !important;
    max-width: 4rem !important;
}

/* 접힌 상태에서 텍스트 숨김 */
[data-testid="stSidebar"].sidebar-collapsed .stMarkdown:not(.keep-visible),
[data-testid="stSidebar"].sidebar-collapsed .stSelectbox,
[data-testid="stSidebar"].sidebar-collapsed .stExpander {
    display: none !important;
}

/* 접힌 상태에서 버튼 중앙 정렬 */
[data-testid="stSidebar"].sidebar-collapsed .stButton > button {
    justify-content: center !important;
    padding: 0.5rem !important;
}

/* Streamlit 네이티브 토글 버튼 숨김 */
button[aria-label*="sidebar"],
[data-testid="stHeader"] button:first-child {
    display: none !important;
}
```

### 3.3 JavaScript 구조 (최소화)

```javascript
// 사이드바 상태 동기화 (단순화)
(function() {
    'use strict';
    
    function syncSidebarState() {
        const sidebar = document.querySelector('[data-testid="stSidebar"]');
        if (!sidebar) return;
        
        const isCollapsed = sidebar.getAttribute('data-sidebar-collapsed') === 'true';
        
        if (isCollapsed) {
            sidebar.classList.add('sidebar-collapsed');
            sidebar.style.width = '4rem';
            sidebar.style.minWidth = '4rem';
            sidebar.style.maxWidth = '4rem';
        } else {
            sidebar.classList.remove('sidebar-collapsed');
            sidebar.style.width = '15rem';
            sidebar.style.minWidth = '15rem';
            sidebar.style.maxWidth = '15rem';
        }
    }
    
    // 초기 실행
    syncSidebarState();
    
    // 주기적 확인 (1초마다)
    setInterval(syncSidebarState, 1000);
    
    // DOM 변경 감지
    const observer = new MutationObserver(function() {
        syncSidebarState();
    });
    
    const sidebar = document.querySelector('[data-testid="stSidebar"]');
    if (sidebar) {
        observer.observe(sidebar, {
            attributes: true,
            attributeFilter: ['data-sidebar-collapsed', 'class']
        });
    }
})();
```

## 4. 구현 계획

### 4.1 Phase 1: 기존 코드 삭제

**삭제할 부분**:
1. `app.py` 1053-1210줄: 기존 사이드바 코드 전체
2. `app.py` 1058-1091줄: `_render_collapsed_sidebar` 함수
3. `app.py` 645-1048줄: 복잡한 사이드바 관련 JavaScript (일부는 유지)
4. `app.py` 334-437줄: 복잡한 사이드바 CSS (필수만 유지)

**유지할 부분**:
1. Material Icons 관련 CSS/JavaScript (아이콘 표시용)
2. 기본 테마 CSS
3. Expander 관련 CSS (화살표 위치)

### 4.2 Phase 2: 새로운 사이드바 구현

1. **함수 정의**
   - `render_sidebar()`: 메인 사이드바 렌더링 함수
   - `render_expanded_sidebar(menu)`: 펼친 상태 렌더링
   - `render_collapsed_sidebar(menu)`: 접힌 상태 렌더링

2. **CSS 추가**
   - 최소한의 CSS만 추가
   - 접기/펼치기 관련 스타일

3. **JavaScript 추가**
   - 단순한 상태 동기화 스크립트만 추가

### 4.3 Phase 3: 통합 및 테스트

1. **기존 기능 확인**
   - 모든 메뉴 버튼이 정상 작동하는지 확인
   - 페이지 라우팅이 정상인지 확인
   - 접기/펼치기 기능 확인

2. **성능 확인**
   - 페이지 로딩 속도 확인
   - JavaScript 실행 시간 확인

## 5. 삭제할 코드 목록

### 5.1 JavaScript 삭제

1. **사이드바 강제 열기 JavaScript** (645-880줄)
   - `maintainSidebarState()` 함수 전체
   - 복잡한 MutationObserver 로직
   - 주기적 실행 로직

2. **사이드바 접기/펼치기 상태 관리 JavaScript** (883-1048줄)
   - 복잡한 상태 관리 로직
   - 중복 실행 방지 로직

3. **사이드바 내부 상태 설정 JavaScript** (1096-1114줄)
   - 중복된 상태 설정 로직

### 5.2 CSS 삭제/간소화

1. **사이드바 관련 CSS** (334-437줄)
   - 복잡한 선택자들 간소화
   - 중복된 스타일 제거

### 5.3 Python 코드 삭제

1. **기존 사이드바 렌더링 코드** (1053-1210줄)
   - `with st.sidebar:` 블록 전체
   - `_render_collapsed_sidebar()` 함수

## 6. 새로운 코드 구조

### 6.1 파일 구조

```
app.py
├── CSS (Material Icons, 기본 테마)
├── JavaScript (Material Icons, 사이드바 상태 동기화)
├── 사이드바 상태 관리
├── 메뉴 구조 정의
├── render_sidebar() 함수
│   ├── render_expanded_sidebar()
│   └── render_collapsed_sidebar()
└── 페이지 라우팅
```

### 6.2 코드 위치

- **CSS**: `app.py` 상단 (기존 Material Icons CSS 유지)
- **JavaScript**: CSS 다음 (간소화된 상태 동기화만)
- **사이드바 렌더링**: `app.py` 중간 (페이지 라우팅 전)

## 7. 예상 결과

### 7.1 개선 사항

1. **코드 간소화**: 약 500줄 이상의 코드 삭제
2. **안정성 향상**: 복잡한 JavaScript 충돌 제거
3. **유지보수성 향상**: 명확한 함수 구조
4. **성능 향상**: 불필요한 JavaScript 실행 제거

### 7.2 기능 유지

- ✅ 접기/펼치기 기능
- ✅ 모든 메뉴 버튼
- ✅ 페이지 라우팅
- ✅ 매장 선택
- ✅ 로그아웃/캐시 클리어

## 8. 구현 체크리스트

### 8.1 삭제 작업
- [ ] 기존 사이드바 렌더링 코드 삭제
- [ ] 복잡한 JavaScript 삭제
- [ ] 중복 CSS 정리

### 8.2 새 코드 작성
- [ ] `render_sidebar()` 함수 작성
- [ ] `render_expanded_sidebar()` 함수 작성
- [ ] `render_collapsed_sidebar()` 함수 작성
- [ ] 간소화된 CSS 작성
- [ ] 간소화된 JavaScript 작성

### 8.3 테스트
- [ ] 모든 메뉴 버튼 동작 확인
- [ ] 접기/펼치기 기능 확인
- [ ] 페이지 라우팅 확인
- [ ] 상태 유지 확인

## 9. 위험 요소 및 대응

### 9.1 위험 요소

1. **기존 기능 손실 가능성**
   - 대응: 단계별 구현 및 테스트

2. **페이지 라우팅 오류**
   - 대응: 기존 라우팅 코드 유지

3. **상태 관리 오류**
   - 대응: session_state 사용 방식 유지

### 9.2 롤백 계획

- Git 커밋으로 이전 버전으로 롤백 가능
- 단계별 구현으로 문제 발생 시 이전 단계로 복귀

## 10. 다음 단계

1. 기획안 검토 및 승인
2. Phase 1: 기존 코드 삭제
3. Phase 2: 새로운 사이드바 구현
4. Phase 3: 테스트 및 검증
5. 최종 배포
