# 사이드바 프리미엄 블랙 테마 업그레이드 기획안 (수정본)

## 📋 목표

**Streamlit 기본 사이드바(st.sidebar)는 그대로 유지. 사이드바 내부 메뉴(카테고리 타이틀/버튼/expander/selectbox/시스템 버튼)를 '프리미엄 블랙 테마'로만 업그레이드**

---

## ⚠️ 절대 금지 사항

1. **JavaScript, DOM 조작, 이벤트 리스너 금지**
   - DOM 조작 코드
   - 이벤트 리스너
   - 동적 스타일 변경

2. **사이드바 DOM 직접 타겟 금지**
   - `[data-testid="stSidebar"]` 기반 CSS
   - 사이드바 위치/크기/토글 관련 CSS
   - 사이드바 전체 구조 변경

3. **전역 CSS 영향 금지**
   - 메인 콘텐츠 영역 버튼/레이아웃에 영향을 주는 전역 CSS
   - `.stButton > button` 같은 전역 규칙을 스코프 없이 사용 금지

---

## ✅ 핵심 원칙

### CSS 스코프 제한
- **CSS는 "사이드바 내부에 삽입되는 래퍼 클래스" 아래로만 스코프 제한**
- `.ps-sidebar-scope` 래퍼 클래스 하위에서만 CSS 적용
- 전역 `.stButton > button` 같은 규칙도 반드시 `.ps-sidebar-scope` 하위에서만 적용

### 구현 방식
- `render_expanded_sidebar()` 함수 내부만 수정
- Streamlit 위젯 기반 구조 유지 (`st.button`, `st.markdown`, `st.expander`, `st.selectbox`)
- CSS는 세션에서 1회만 주입

---

## 📊 현재 상태 분석

### 현재 사이드바 구조
```python
def render_expanded_sidebar(menu):
    # 매장 선택
    st.selectbox("🏪 매장 선택", ...)
    
    # 메뉴 렌더링
    for cat, data in menu.items():
        st.markdown(f"**{cat}**")  # 카테고리 제목
        if isinstance(data, list):
            # 단순 리스트 버튼
            st.button(label, ...)
        else:
            # Main 항목 버튼
            st.button(label, ...)
            # Sub 항목 (expander 내부)
            with st.expander("상세 선택"):
                st.button(label, ...)
    
    # 로그아웃, 캐시 클리어
    st.button("🚪 로그아웃")
    st.button("🔄 캐시 클리어")
```

---

## 🎨 목표 디자인 (프리미엄 블랙 테마)

### 색상 팔레트
- **배경 톤**: #0F172A (Slate 950) / #1E293B (Slate 800)
- **버튼 기본 배경**: rgba(255, 255, 255, 0.04)
- **버튼 호버 배경**: rgba(255, 255, 255, 0.08)
- **액센트 블루**: #3B82F6 (Blue 500) / #2563EB (Blue 600)
- **텍스트 주**: #E2E8F0 (Slate 200)
- **텍스트 보조**: #94A3B8 (Slate 400)
- **경계선**: rgba(255, 255, 255, 0.1)

### 스타일 요구사항
- **모서리**: 10~14px (border-radius)
- **여백**: 넉넉하게 (padding, margin)
- **호버 효과**: 
  - 배경색 변화 (rgba(255,255,255,0.08))
  - border 강조
  - subtle glow
  - **transform translateX 금지** (레이아웃 흔들림 방지)
- **활성 버튼**: 블루 그라데이션 + glow 효과

---

## 🔧 구현 방법

### 1. 스코프 래퍼 추가

**위치**: `render_expanded_sidebar()` 함수 시작/끝 부분

```python
def render_expanded_sidebar(menu):
    # CSS 주입 (한 번만)
    if "premium_sidebar_css_injected" not in st.session_state:
        st.session_state.premium_sidebar_css_injected = True
        st.markdown("""
        <style>
        /* 프리미엄 블랙 테마 CSS - .ps-sidebar-scope 하위만 */
        </style>
        """, unsafe_allow_html=True)
    
    # 스코프 래퍼 시작
    st.markdown('<div class="ps-sidebar-scope">', unsafe_allow_html=True)
    
    # 기존 사이드바 UI 렌더링
    # ... (모든 사이드바 콘텐츠)
    
    # 스코프 래퍼 종료
    st.markdown('</div>', unsafe_allow_html=True)
```

### 2. CSS 선택자 전략

**모든 CSS는 `.ps-sidebar-scope` 하위로 제한**:

```css
/* ✅ 올바른 방법: 스코프 제한 */
.ps-sidebar-scope .stButton > button {
    /* 버튼 스타일 */
}

.ps-sidebar-scope .stMarkdown {
    /* 마크다운 스타일 */
}

.ps-sidebar-scope .premium-category-title {
    /* 카테고리 제목 */
}

/* ❌ 금지: 전역 선택자 */
.stButton > button {
    /* 이렇게 하면 메인 콘텐츠에도 영향 */
}
```

---

## 🎯 상세 구현 계획

### Phase 1: 스코프 래퍼 및 CSS 주입

#### 1.1 스코프 래퍼 추가
```python
def render_expanded_sidebar(menu):
    # CSS 주입 (세션에서 1회만)
    if "premium_sidebar_css_injected" not in st.session_state:
        st.session_state.premium_sidebar_css_injected = True
        st.markdown("""
        <style>
        /* 프리미엄 블랙 테마 CSS */
        </style>
        """, unsafe_allow_html=True)
    
    # 스코프 래퍼 시작
    st.markdown('<div class="ps-sidebar-scope">', unsafe_allow_html=True)
    
    # ... 기존 코드 ...
    
    # 스코프 래퍼 종료
    st.markdown('</div>', unsafe_allow_html=True)
```

#### 1.2 CSS 작성 (스코프 제한)
```css
/* 카테고리 제목 */
.ps-sidebar-scope .premium-category-title {
    color: #94A3B8;
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-top: 1.5rem;
    margin-bottom: 0.5rem;
    padding: 0 0.5rem;
}

/* 버튼 기본 스타일 */
.ps-sidebar-scope .stButton > button {
    background: rgba(255, 255, 255, 0.04);
    color: #E2E8F0;
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 12px;
    padding: 0.875rem 1rem;
    transition: all 0.2s ease;
    font-weight: 500;
    margin-bottom: 0.5rem;
}

/* 버튼 호버 효과 (transform 금지) */
.ps-sidebar-scope .stButton > button:hover {
    background: rgba(255, 255, 255, 0.08);
    border-color: rgba(255, 255, 255, 0.2);
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
}

/* 활성 버튼 (primary type) */
.ps-sidebar-scope .stButton > button[kind="primary"] {
    background: linear-gradient(135deg, #3B82F6 0%, #2563EB 100%);
    border-color: #3B82F6;
    box-shadow: 0 4px 12px rgba(59, 130, 246, 0.3);
    color: #FFFFFF;
}

/* 비활성 버튼 (secondary type) */
.ps-sidebar-scope .stButton > button[kind="secondary"] {
    background: rgba(255, 255, 255, 0.04);
    color: #E2E8F0;
}

/* Expander 스타일 */
.ps-sidebar-scope .stExpander {
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 12px;
    margin-top: 0.5rem;
    margin-bottom: 0.5rem;
}

.ps-sidebar-scope .stExpander > label {
    color: #E2E8F0;
    font-weight: 500;
}

/* Expander 내부 버튼 */
.ps-sidebar-scope .stExpander .stButton > button {
    background: rgba(255, 255, 255, 0.03);
    border-radius: 10px;
    padding: 0.75rem 1rem;
}

.ps-sidebar-scope .stExpander .stButton > button:hover {
    background: rgba(255, 255, 255, 0.06);
}

/* Selectbox 스타일 */
.ps-sidebar-scope .stSelectbox > div > div {
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 12px;
    color: #E2E8F0;
}

.ps-sidebar-scope .stSelectbox label {
    color: #E2E8F0;
    font-weight: 500;
}

/* 시스템 버튼 섹션 */
.ps-sidebar-scope .premium-system-section {
    margin-top: 2rem;
    padding-top: 1.5rem;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
}

.ps-sidebar-scope .premium-system-section .stButton > button {
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 12px;
}

.ps-sidebar-scope .premium-system-section .stButton > button:hover {
    background: rgba(255, 255, 255, 0.08);
    border-color: rgba(255, 255, 255, 0.2);
}
```

### Phase 2: 카테고리 타이틀 업그레이드

```python
# Before
st.markdown(f"**{cat}**")

# After
st.markdown(
    f'<div class="premium-category-title">{cat}</div>',
    unsafe_allow_html=True
)
```

### Phase 3: 버튼 로직 유지

```python
# 기존 로직 그대로 유지
is_active = st.session_state.current_page == key
button_type = "primary" if is_active else "secondary"

if st.button(label, key=f"btn_{key}", use_container_width=True, type=button_type):
    st.session_state.current_page = key
    st.rerun()
```

### Phase 4: 시스템 버튼 스타일링

```python
# HTML wrapper 추가
st.markdown('<div class="premium-system-section">', unsafe_allow_html=True)
st.button("🚪 로그아웃")
st.button("🔄 캐시 클리어")
st.markdown('</div>', unsafe_allow_html=True)
```

---

## 🔍 구현 세부사항

### 1. 스코프 래퍼 구조

```python
def render_expanded_sidebar(menu):
    # CSS 주입 (세션에서 1회만)
    if "premium_sidebar_css_injected" not in st.session_state:
        st.session_state.premium_sidebar_css_injected = True
        st.markdown("""
        <style>
        /* 모든 CSS는 .ps-sidebar-scope 하위로 제한 */
        .ps-sidebar-scope .stButton > button { ... }
        .ps-sidebar-scope .premium-category-title { ... }
        /* ... */
        </style>
        """, unsafe_allow_html=True)
    
    # 스코프 래퍼 시작
    st.markdown('<div class="ps-sidebar-scope">', unsafe_allow_html=True)
    
    # 매장 선택
    user_stores = get_user_stores()
    # ... 기존 코드 ...
    
    # 메뉴 렌더링
    for cat, data in menu.items():
        # 카테고리 제목 (HTML div로 변경)
        st.markdown(
            f'<div class="premium-category-title">{cat}</div>',
            unsafe_allow_html=True
        )
        # ... 기존 버튼 로직 ...
    
    # 시스템 버튼 (wrapper 추가)
    st.markdown('<div class="premium-system-section">', unsafe_allow_html=True)
    st.button("🚪 로그아웃")
    st.button("🔄 캐시 클리어")
    st.markdown('</div>', unsafe_allow_html=True)
    
    # 스코프 래퍼 종료
    st.markdown('</div>', unsafe_allow_html=True)
```

### 2. CSS 상세 스펙

#### 2.1 카테고리 제목
```css
.ps-sidebar-scope .premium-category-title {
    color: #94A3B8;
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-top: 1.5rem;
    margin-bottom: 0.75rem;
    padding: 0 0.5rem;
}
```

#### 2.2 버튼 기본 스타일
```css
.ps-sidebar-scope .stButton > button {
    background: rgba(255, 255, 255, 0.04);
    color: #E2E8F0;
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 12px;
    padding: 0.875rem 1rem;
    transition: all 0.2s ease;
    font-weight: 500;
    font-size: 0.875rem;
    margin-bottom: 0.5rem;
    width: 100%;
    text-align: left;
}
```

#### 2.3 버튼 호버 효과 (transform 금지)
```css
.ps-sidebar-scope .stButton > button:hover {
    background: rgba(255, 255, 255, 0.08);
    border-color: rgba(255, 255, 255, 0.2);
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
    /* transform translateX 금지 - 레이아웃 흔들림 방지 */
}
```

#### 2.4 활성 버튼
```css
.ps-sidebar-scope .stButton > button[kind="primary"] {
    background: linear-gradient(135deg, #3B82F6 0%, #2563EB 100%);
    border-color: #3B82F6;
    box-shadow: 0 4px 12px rgba(59, 130, 246, 0.3);
    color: #FFFFFF;
}
```

#### 2.5 Expander 스타일
```css
.ps-sidebar-scope .stExpander {
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 12px;
    margin-top: 0.5rem;
    margin-bottom: 0.5rem;
}

.ps-sidebar-scope .stExpander .stButton > button {
    background: rgba(255, 255, 255, 0.03);
    border-radius: 10px;
}
```

#### 2.6 Selectbox 스타일
```css
.ps-sidebar-scope .stSelectbox > div > div {
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 12px;
    color: #E2E8F0;
}
```

#### 2.7 시스템 버튼 섹션
```css
.ps-sidebar-scope .premium-system-section {
    margin-top: 2rem;
    padding-top: 1.5rem;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
}

.ps-sidebar-scope .premium-system-section .stButton > button {
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid rgba(255, 255, 255, 0.1);
}
```

---

## 🚀 실행 계획

### Phase 1: 스코프 래퍼 및 CSS 주입
1. `render_expanded_sidebar()` 함수 시작 부분에 CSS 주입 코드 추가
2. 스코프 래퍼 `<div class="ps-sidebar-scope">` 시작 추가
3. 프리미엄 블랙 테마 CSS 작성 (모든 선택자를 `.ps-sidebar-scope` 하위로 제한)
4. 스코프 래퍼 종료 `</div>` 추가

### Phase 2: 카테고리 타이틀 업그레이드
1. `st.markdown(f"**{cat}**")` → HTML div + 클래스로 변경
2. CSS로 프리미엄 스타일 적용

### Phase 3: 버튼 스타일 업그레이드
1. 버튼 로직은 기존 유지 (is_active → type="primary"/"secondary")
2. CSS로 스타일링 (스코프 제한)
3. 호버 효과 추가 (transform 금지)

### Phase 4: Expander 및 Selectbox 스타일링
1. Expander 스타일 적용 (스코프 제한)
2. Selectbox 스타일 적용 (스코프 제한)

### Phase 5: 시스템 버튼 스타일링
1. 시스템 버튼 wrapper 추가
2. 하단 구분선 및 스타일 적용

### Phase 6: 최종 검증
1. 사이드바 항상 보임 확인
2. 모든 버튼 클릭 동작 확인
3. 페이지 라우팅 정상 확인
4. 매장 선택 기능 확인
5. 로그아웃 기능 확인
6. **메인 콘텐츠 버튼/레이아웃 스타일 변화 없음 확인 (중요)**

---

## 📊 예상 결과

### Before (현재)
- 기본 Streamlit 밝은 테마
- 평면적인 버튼 디자인
- 단순한 카테고리 제목
- 기본적인 선택 상태 표시

### After (목표)
- ✅ 고급스러운 블랙 테마
- ✅ 프리미엄 느낌의 버튼 디자인
- ✅ 세련된 카테고리 제목
- ✅ 명확한 선택 상태 표시
- ✅ 부드러운 호버 효과 (transform 없음)
- ✅ 미니멀한 계층 구조
- ✅ **메인 콘텐츠에 영향 없음**

---

## ⚠️ 주의사항

### 1. CSS 스코프 제한 (최우선)
- **모든 CSS는 `.ps-sidebar-scope` 하위로 제한**
- 전역 선택자 절대 사용 금지
- 메인 콘텐츠에 영향 없도록 주의

### 2. Transform 금지
- 호버 시 `transform: translateX()` 사용 금지
- 레이아웃 흔들림 방지
- 대신 배경색, border, box-shadow만 사용

### 3. Streamlit 위젯 호환성
- `st.button`의 기본 동작 유지
- `type="primary"/"secondary"` 속성 활용
- 위젯 키 충돌 방지

### 4. 성능 고려
- CSS는 세션에서 1회만 주입
- 불필요한 CSS 중복 방지
- 최소한의 CSS만 사용

---

## 📌 체크리스트

### 구현 확인
- [ ] CSS 주입 코드 추가 (세션에서 1회만)
- [ ] 스코프 래퍼 `<div class="ps-sidebar-scope">` 추가
- [ ] 모든 CSS 선택자를 `.ps-sidebar-scope` 하위로 제한
- [ ] 카테고리 제목 HTML + CSS 스타일링
- [ ] 버튼 기본 스타일 적용 (스코프 제한)
- [ ] 활성 버튼 스타일 (primary, 스코프 제한)
- [ ] 비활성 버튼 스타일 (secondary, 스코프 제한)
- [ ] 호버 효과 추가 (transform 없음)
- [ ] Expander 스타일 적용 (스코프 제한)
- [ ] Selectbox 스타일 적용 (스코프 제한)
- [ ] 시스템 버튼 스타일 적용 (스코프 제한)
- [ ] 스코프 래퍼 종료 `</div>` 추가

### 제약사항 준수 확인
- [ ] JavaScript 사용 안 함
- [ ] DOM 조작 안 함
- [ ] `[data-testid="stSidebar"]` 선택자 사용 안 함
- [ ] 사이드바 위치/크기 변경 안 함
- [ ] **전역 CSS 사용 안 함 (모든 CSS가 `.ps-sidebar-scope` 하위)**
- [ ] **메인 콘텐츠 버튼/레이아웃 스타일 변화 없음**

### 기능 확인
- [ ] 사이드바 항상 보임
- [ ] 모든 버튼 클릭 동작 정상
- [ ] 페이지 라우팅 정상
- [ ] 선택 상태 표시 정상
- [ ] 매장 선택 기능 정상
- [ ] 로그아웃 기능 정상
- [ ] 캐시 클리어 기능 정상

---

## 🎨 디자인 상세 스펙

### 색상 팔레트
- **배경 톤**: #0F172A (Slate 950) / #1E293B (Slate 800)
- **버튼 기본**: rgba(255, 255, 255, 0.04)
- **버튼 호버**: rgba(255, 255, 255, 0.08)
- **액센트 블루**: #3B82F6 / #2563EB
- **텍스트 주**: #E2E8F0
- **텍스트 보조**: #94A3B8
- **경계선**: rgba(255, 255, 255, 0.1)

### 스타일 값
- **Border Radius**: 12px (버튼), 10px (Expander 내부)
- **Padding**: 0.875rem 1rem (버튼)
- **Margin**: 0.5rem ~ 1.5rem
- **Shadow**: 
  - 호버: 0 2px 8px rgba(0, 0, 0, 0.2)
  - 활성: 0 4px 12px rgba(59, 130, 246, 0.3)
- **Transition**: all 0.2s ease
- **Transform**: **사용 금지** (레이아웃 흔들림 방지)

---

**작성일**: 2026-01-25  
**목적**: 사이드바 내부 UI를 프리미엄 블랙 테마로 업그레이드 (스코프 제한, 제약사항 준수)
