# 건강검진 → 매장 체크리스트 변경 완료 보고서

**작업 일자**: 2026-01-24  
**목적**: "건강검진"을 입력 그룹으로 이동하고 명칭을 "매장 체크리스트"로 통일 (기능/DB/로직 변경 없이 라벨·분류만 수정)

---

## 1️⃣ 사이드바 구조 변경 요약

### 변경 전

```
📊 분석
  - 건강검진 실시
  - 검진 결과 요약
  - 검진 히스토리
  - 매출 하락 원인 찾기
  - 매출 분석
  - 판매·메뉴 분석
  - 원가 분석
```

### 변경 후

```
✍ 입력
  - 입력 허브
  - 일일 입력(통합)
  - 점장 마감
  - 매출 등록
  - 판매량 등록
  - 월간 성적표
  - 주간 리포트
  - 매장 체크리스트 (신규 이동)
  - 체크 결과 (신규 이동)
  - 체크 히스토리 (신규 이동)

📊 분석
  - 매출 하락 원인 찾기
  - 매출 분석
  - 판매·메뉴 분석
  - 원가 분석
```

---

## 2️⃣ 변경된 라벨 목록

### 사이드바 메뉴 라벨

| 변경 전 | 변경 후 | Page Key (유지) |
|---------|---------|----------------|
| 건강검진 실시 | 매장 체크리스트 | "건강검진 실시" |
| 검진 결과 요약 | 체크 결과 | "검진 결과 요약" |
| 검진 히스토리 | 체크 히스토리 | "검진 히스토리" |

### 입력 허브 버튼

| 변경 전 | 변경 후 | Page Key (유지) |
|---------|---------|----------------|
| 🩺 건강검진 | 📋 매장 체크리스트 | "건강검진 실시" |

### 페이지 타이틀/헤더

| 파일 | 변경 전 | 변경 후 |
|------|---------|---------|
| health_check_page.py | "가게 건강검진" | "매장 체크리스트" |
| health_check_result.py | "검진 결과 요약" | "체크 결과 요약" |
| health_check_history.py | "검진 히스토리" | "체크 히스토리" |

### 페이지 내부 문구

**health_check_page.py**:
- "건강검진" → "매장 체크리스트"
- "검진" → "체크"
- "새 검진 시작" → "새 체크 시작"
- "검진 완료" → "체크 완료"
- "검진 결과" → "체크 결과"
- "검진 이력" → "체크 이력"

**health_check_result.py**:
- "검진 결과 요약" → "체크 결과 요약"
- "건강검진" → "매장 체크리스트"
- "검진" → "체크"
- "최근 검진" → "최근 체크"
- "이전 검진" → "이전 체크"
- "다음 검진" → "다음 체크"

**health_check_history.py**:
- "검진 히스토리" → "체크 히스토리"
- "건강검진" → "매장 체크리스트"
- "검진" → "체크"
- "검진 회차" → "체크 회차"

---

## 3️⃣ 수정된 파일 목록

### 3.1 사이드바 메뉴

**파일**: `app.py`
- **위치**: 라인 1516-1534
- **변경 내용**:
  - "건강검진 실시", "검진 결과 요약", "검진 히스토리"를 분석 그룹에서 입력 그룹으로 이동
  - 라벨 변경: "건강검진 실시" → "매장 체크리스트", "검진 결과 요약" → "체크 결과", "검진 히스토리" → "체크 히스토리"
  - page key는 기존 유지 ("건강검진 실시", "검진 결과 요약", "검진 히스토리")

### 3.2 입력 허브

**파일**: `ui_pages/input/input_hub.py`
- **위치**: 라인 142-145
- **변경 내용**:
  - 버튼 라벨: "🩺 건강검진" → "📋 매장 체크리스트"
  - page key는 기존 유지 ("건강검진 실시")

### 3.3 페이지 타이틀/헤더

**파일**: `ui_pages/health_check/health_check_page.py`
- **변경 내용**:
  - 파일 주석: "건강검진 페이지" → "매장 체크리스트 페이지"
  - 함수 docstring: "건강검진 페이지 렌더링" → "매장 체크리스트 페이지 렌더링"
  - 페이지 헤더: "가게 건강검진" → "매장 체크리스트", 아이콘: "🩺" → "📋"
  - 페이지 설명: "정기 건강검진" → "정기 매장 체크리스트"
  - 모든 "검진" → "체크" 변경 (버튼, 메시지, 탭 등)

**파일**: `ui_pages/health_check/health_check_result.py`
- **변경 내용**:
  - 파일 주석: "검진 결과 요약 페이지" → "체크 결과 요약 페이지"
  - 함수 docstring: "검진 결과 요약 페이지 렌더링" → "체크 결과 요약 페이지 렌더링"
  - 페이지 헤더: "검진 결과 요약" → "체크 결과 요약", 아이콘: "🩺" → "📋"
  - 모든 "검진" → "체크" 변경
  - "건강검진" → "매장 체크리스트" 변경

**파일**: `ui_pages/health_check/health_check_history.py`
- **변경 내용**:
  - 파일 주석: "검진 히스토리 페이지" → "체크 히스토리 페이지"
  - 함수 docstring: "검진 히스토리 페이지 렌더링" → "체크 히스토리 페이지 렌더링"
  - 페이지 헤더: "검진 히스토리" → "체크 히스토리"
  - 모든 "검진" → "체크" 변경
  - "건강검진" → "매장 체크리스트" 변경

---

## 4️⃣ 기능 영향 없음 체크리스트

### ✅ Page Key 유지
- [x] "건강검진 실시" 키 유지 (라우팅 키)
- [x] "검진 결과 요약" 키 유지 (라우팅 키)
- [x] "검진 히스토리" 키 유지 (라우팅 키)
- [x] 모든 내부 이동에서 page key 유지

### ✅ 함수명/파일명 유지
- [x] 함수명 변경 없음 (`render_health_check_page`, `render_health_check_result`, `render_health_check_history`)
- [x] 파일명 변경 없음 (`health_check_page.py`, `health_check_result.py`, `health_check_history.py`)
- [x] 모듈 경로 변경 없음 (`ui_pages/health_check/`)

### ✅ DB/테이블명 유지
- [x] 테이블명 변경 없음 (health_sessions, health_answers 등)
- [x] DB 쿼리 변경 없음
- [x] 저장 로직 변경 없음

### ✅ 로직 변경 없음
- [x] 페이지 렌더링 로직 변경 없음
- [x] 데이터 처리 로직 변경 없음
- [x] 라우팅 로직 변경 없음

### ✅ 라우팅 유지
- [x] `elif page == "건강검진 실시":` 유지
- [x] `elif page == "검진 결과 요약":` 유지
- [x] `elif page == "검진 히스토리":` 유지

---

## 5️⃣ 검증 체크리스트

### 사이드바 메뉴
- [ ] 입력 그룹에 "매장 체크리스트", "체크 결과", "체크 히스토리" 표시됨
- [ ] 분석 그룹에서 건강검진 관련 항목 제거됨
- [ ] 각 메뉴 클릭 시 해당 페이지로 정상 이동

### 입력 허브
- [ ] "주간·불시" 섹션에 "📋 매장 체크리스트" 버튼 표시됨
- [ ] 버튼 클릭 시 "건강검진 실시" 페이지로 이동 (page key 유지)

### 페이지 타이틀/헤더
- [ ] health_check_page.py: "매장 체크리스트" 헤더 표시
- [ ] health_check_result.py: "체크 결과 요약" 헤더 표시
- [ ] health_check_history.py: "체크 히스토리" 헤더 표시

### 페이지 내부 문구
- [ ] 모든 "건강검진" → "매장 체크리스트" 변경 확인
- [ ] 모든 "검진" → "체크" 변경 확인
- [ ] 버튼 문구 변경 확인
- [ ] 메시지 문구 변경 확인

---

## 6️⃣ 변경 상세 내역

### 6.1 app.py 변경

**위치**: 라인 1516-1534

**변경 전**:
```python
"✍ 입력": [
    ...
],
"📊 분석": [
    ("건강검진 실시", "건강검진 실시"),
    ("검진 결과 요약", "검진 결과 요약"),
    ("검진 히스토리", "검진 히스토리"),
    ...
],
```

**변경 후**:
```python
"✍ 입력": [
    ...
    ("매장 체크리스트", "건강검진 실시"),  # 라벨 변경, page key 유지
    ("체크 결과", "검진 결과 요약"),  # 라벨 변경, page key 유지
    ("체크 히스토리", "검진 히스토리"),  # 라벨 변경, page key 유지
],
"📊 분석": [
    # 건강검진 관련 항목 제거됨
    ...
],
```

### 6.2 input_hub.py 변경

**위치**: 라인 142-145

**변경 전**:
```python
if st.button("🩺 건강검진", ...):
    st.session_state["current_page"] = "건강검진 실시"
```

**변경 후**:
```python
if st.button("📋 매장 체크리스트", ...):
    st.session_state["current_page"] = "건강검진 실시"  # page key 유지
```

### 6.3 health_check_page.py 변경

**주요 변경 사항**:
- 페이지 헤더: "가게 건강검진" → "매장 체크리스트", 아이콘: "🩺" → "📋"
- 페이지 설명: "정기 건강검진" → "정기 매장 체크리스트"
- "새 검진 시작" → "새 체크 시작"
- "검진 완료" → "체크 완료"
- "검진 결과" → "체크 결과"
- "검진 이력" → "체크 이력"
- 탭: "검진 입력" → "체크 입력", "검진 이력" → "체크 이력"

### 6.4 health_check_result.py 변경

**주요 변경 사항**:
- 페이지 헤더: "검진 결과 요약" → "체크 결과 요약", 아이콘: "🩺" → "📋"
- "최근 검진" → "최근 체크"
- "이전 검진" → "이전 체크"
- "다음 검진" → "다음 체크"
- "건강검진 실시하기" → "매장 체크리스트 실시하기"
- "건강검진 다시하기" → "매장 체크리스트 다시하기"
- "검진 히스토리 보기" → "체크 히스토리 보기"

### 6.5 health_check_history.py 변경

**주요 변경 사항**:
- 페이지 헤더: "검진 히스토리" → "체크 히스토리"
- "검진 회차 리스트" → "체크 회차 리스트"
- "건강검진 실시하기" → "매장 체크리스트 실시하기"
- "완료된 검진이 없습니다" → "완료된 체크가 없습니다"
- "첫 건강검진" → "첫 매장 체크리스트"

---

## 7️⃣ 주의 사항

### 7.1 Page Key 유지

**중요**: 모든 라우팅에서 page key는 기존 그대로 유지됩니다.

- 사이드바: 라벨만 변경, page key는 "건강검진 실시", "검진 결과 요약", "검진 히스토리" 유지
- 입력 허브: 버튼 라벨만 변경, page key는 "건강검진 실시" 유지
- 내부 이동: 모든 버튼에서 page key는 기존 유지

**이유**: 라우팅 로직(`elif page == "건강검진 실시"`)과의 호환성 유지

### 7.2 함수명/파일명 유지

**중요**: 함수명과 파일명은 변경하지 않았습니다.

- 함수명: `render_health_check_page()`, `render_health_check_result()`, `render_health_check_history()` 유지
- 파일명: `health_check_page.py`, `health_check_result.py`, `health_check_history.py` 유지
- 모듈 경로: `ui_pages/health_check/` 유지

**이유**: import 경로와 함수 호출과의 호환성 유지

### 7.3 DB/테이블명 유지

**중요**: DB 테이블명과 쿼리는 변경하지 않았습니다.

- 테이블명: `health_sessions`, `health_answers` 등 유지
- 쿼리 로직: 변경 없음
- 저장 로직: 변경 없음

**이유**: 기존 데이터와의 호환성 유지

---

## 8️⃣ 테스트 시나리오

### 시나리오 1: 사이드바 메뉴 이동
1. 사이드바 입력 그룹에서 "매장 체크리스트" 클릭
2. **예상 결과**: "건강검진 실시" 페이지로 이동 (page key 유지)
3. 페이지 헤더: "매장 체크리스트" 표시

### 시나리오 2: 입력 허브에서 이동
1. 입력 허브 페이지에서 "📋 매장 체크리스트" 버튼 클릭
2. **예상 결과**: "건강검진 실시" 페이지로 이동 (page key 유지)
3. 페이지 헤더: "매장 체크리스트" 표시

### 시나리오 3: 페이지 내부 문구 확인
1. 매장 체크리스트 페이지 접근
2. **예상 결과**: 모든 "건강검진" → "매장 체크리스트", "검진" → "체크" 변경 확인
3. 버튼: "새 체크 시작", "체크 완료" 등 표시

### 시나리오 4: 체크 결과 페이지 확인
1. 체크 결과 페이지 접근
2. **예상 결과**: "체크 결과 요약" 헤더 표시
3. 모든 "검진" → "체크" 변경 확인

### 시나리오 5: 체크 히스토리 페이지 확인
1. 체크 히스토리 페이지 접근
2. **예상 결과**: "체크 히스토리" 헤더 표시
3. 모든 "검진" → "체크" 변경 확인

---

**작성일**: 2026-01-24  
**담당**: 건강검진 → 매장 체크리스트 변경 작업
