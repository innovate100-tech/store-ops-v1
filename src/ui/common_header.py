"""
공통 헤더 컴포넌트 (제목 박스 + 전광판)
[비활성화됨] - 더 이상 사용하지 않음
"""
import streamlit as st

# 이 모듈은 더 이상 사용되지 않습니다.
# 호환성을 위해 함수는 유지하되, 아무것도 렌더링하지 않습니다.


def render_common_header(marquee_text: str | None = None):
    """
    [비활성화됨] 공통 헤더 렌더링 함수
    
    이 함수는 더 이상 사용되지 않습니다.
    호환성을 위해 유지되지만 아무것도 렌더링하지 않습니다.
    
    Args:
        marquee_text: 전광판 텍스트 (무시됨)
    """
    # 아무것도 렌더링하지 않음
    pass
